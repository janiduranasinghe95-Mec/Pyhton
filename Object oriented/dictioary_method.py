student = {
    "name": "Nimal",
    "age": 20,
    "city": "Colombo"
}

student.get("name")

student.items()

student.keys()

student.values()

nw_dict = dict.fromkeys(["A", "B", "C"], 0)

print(nw_dict)